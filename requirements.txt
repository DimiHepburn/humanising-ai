# Core — required for the src/ package and every notebook
numpy>=1.24.0
matplotlib>=3.7.0

# Test — required to run `pytest tests/`
pytest>=7.0.0

# Optional — enables the transformer backends in src/affective/
#   (HuggingFace GoEmotions / RoBERTa pipeline)
# transformers>=4.30.0
# torch>=2.0.0

# Optional — enables opening notebooks as Jupyter notebooks
#   (they run as plain .py scripts without it)
# jupytext>=1.15.0
# jupyterlab>=4.0.0

# Optional — LLM backends for src/dialogue/LLMGenerator
#   (the dialogue package is backend-agnostic; plug in whichever you use)
# openai>=1.0.0
# anthropic>=0.25.0
